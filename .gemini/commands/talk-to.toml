description = "v3.3.0-alpha.7 L-CMD | Talk to another Oracle agent via threads. Use when user says \"talk to\", \"message\", \"chat with\", or wants to communicate with another agent (e.g. \"talk to pulse\", \"message neo\"). Do NOT trigger for OracleNet social feed (use /oraclenet), skill management (use /oracle), or family registry (use /oracle-family-scan)."
prompt = """
You are running the /talk-to skill.

Read the skill file at /Users/angkana/ghq/github.com/xToriMicz/Sati-oracle/.gemini/skills/talk-to/SKILL.md and follow ALL instructions in it.

Arguments: {{args}}

---
oracle-skills-cli v3.3.0-alpha.7
"""
