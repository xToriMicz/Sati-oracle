description = "v3.3.0-alpha.7 L-CMD | Session orientation and awareness — retro summaries, handoffs, git state, focus. Use when starting a session, after /jump, lost your place, switching context, or when user asks \"now\", \"where are we\", \"what are we doing\", \"status\", \"recap\". Do NOT trigger for \"standup\" or \"morning check\" (use /standup), or session mining \"dig\", \"past sessions\" (use /dig)."
prompt = """
You are running the /recap skill.

Read the skill file at /Users/angkana/ghq/github.com/xToriMicz/Sati-oracle/.gemini/skills/recap/SKILL.md and follow ALL instructions in it.

Arguments: {{args}}

---
oracle-skills-cli v3.3.0-alpha.7
"""
